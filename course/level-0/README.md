# Блок 0. Введение и настройка инструментов

Добро пожаловать в курс «AI-инженер на практике: от кастомных MCP до терминального оркестратора». Этот блок — твой пропуск в мир AI-агентов, где LLM не просто болтает, а выполняет реальные задачи: проектирует детали в Компас-3D и отправляет их на лазерную гравировку. Мы начинаем с нуля: разберём архитектуру, поставим инструменты и впервые пообщаемся с моделями DeepSeek и YandexGPT через оркестратор OpenCode. Поехали!

---

## Модуль 0.1 — Цели курса и обзор архитектуры

### 1.1 Теория: какую проблему решаем и из чего состоит система

**Проблема:** инженеру или производителю нужно быстро превратить идею в готовое изделие. Обычно это цепочка «спроектировал в CAD → экспортировал → передал на станок». Но что, если ты можешь написать текстом «сделай мне прямоугольную пластину с отверстиями по углам» и получить готовый файл для лазерного гравировщика? Именно это мы автоматизируем с помощью AI-агентов.

**Архитектура системы (три ключевых компонента):**

1. **Кастомные MCP-серверы**  
   - **Компас-3D MCP-сервер** — управляет российской CAD-системой КОМПАС-3D: создаёт эскизы, выдавливает 3D-модели, экспортирует DXF.  
   - **CarverAll 15Pro MCP-сервер** — общается с лазерным гравировщиком CarverAll: отправляет файл на резку, следит за статусом, возвращает результат.

2. **Терминальный оркестратор OpenCode**  
   Твой «командир» в мире AI. Он принимает задачу на естественном языке, планирует действия, вызывает нужные MCP-инструменты и следит за результатом.

3. **Большая языковая модель (LLM)**  
   Понимает твои запросы, решает, какие инструменты нужны, и генерирует корректные вызовы. Мы используем open-source модели: DeepSeek-V3 и YandexGPT.

**Как это работает в типичном сценарии:**
```
Ты: «Создай квадратную заготовку 100x100 мм с отверстием 10 мм по центру и вырежи её на гравировщике»
OpenCode -> LLM: запрос на планирование
LLM -> OpenCode: «Нужно: 1) создать эскиз в Компас-3D, 2) выдавить, 3) экспортировать в DXF, 4) отправить на CarverAll»
OpenCode -> kompas3d-mcp: вызов tool "create_sketch"
kompas3d-mcp -> OpenCode: success
...
OpenCode -> carverall-mcp: вызов tool "start_job"
carverall-mcp -> OpenCode: job_id=42
OpenCode -> Ты: «Готово! Заготовка отправлена, статус можно проверить по ID 42»
```

**Почему мы строим всё на open-source стеке?**
- **Независимость** от вендоров и санкционных ограничений.
- **Низкая стоимость** — DeepSeek стоит копейки (а YandexGPT вообще российский), свои MCP-серверы крутятся локально.
- **Полный контроль** над данными и логикой работы. Ты можешь кастомизировать любой компонент.

### 1.2 Установка обязательного окружения

Нам понадобятся Node.js, Git и (опционально) Docker. Ты уже опытный разработчик, поэтому кратко пробежимся по командам проверки.

**Node.js 20+ и npm**
- macOS/Linux (рекомендуется через nvm):
  ```bash
  nvm install 20
  nvm use 20
  node -v   # должно быть v20.x.x
  npm -v    # должно быть 10.x.x или выше
  ```
- Windows: скачай установщик LTS с [nodejs.org](https://nodejs.org/), при установке обязательно отметь галочку «Add to PATH». Проверка та же.

**Git**
- macOS/Linux: обычно уже есть. Если нет — `sudo apt install git` (Ubuntu) или `brew install git` (macOS).
- Windows: скачай с [git-scm.com](https://git-scm.com/).  
Проверка:
```bash
git --version   # git version 2.40+
```

**Docker (рекомендуется)**
Нам Docker пригодится для локального запуска MCP-серверов и изоляции.  
- Установи Docker Desktop с [docker.com](https://www.docker.com/products/docker-desktop/)  
- Проверка:
  ```bash
  docker --version   # Docker version 24+
  docker compose version  # должно работать
  ```
Если Docker не нужен / не ставится — ничего страшного, MCP-серверы можно запускать и напрямую через Node.js. Но в курсе мы будем использовать Docker для единообразия.

### 1.3 Создание структуры монорепозитория

Наш проект будет монорепозиторием с тремя основными папками:

```
ai-engineer-course/
├── kompas3d-mcp/        # MCP-сервер для Компас-3D
├── carverall-mcp/       # MCP-сервер для гравировщика
├── opencode-config/     # Конфиги оркестратора и скрипты
├── .gitignore
└── README.md
```

Создадим его:

```bash
mkdir ai-engineer-course
cd ai-engineer-course
mkdir kompas3d-mcp carverall-mcp opencode-config
```

Инициализируем Git и сразу добавим `.gitignore` для Node.js:
```bash
git init
echo "node_modules/
dist/
.env
*.log" > .gitignore
```

Добавим пустой `README.md` и создадим общий `package.json` (пока чисто для информации):
```bash
echo "# AI-инженер на практике" > README.md
cd opencode-config
# В этом модуле package.json не обязателен, но создадим для порядка
npm init -y
cd ..
```

Теперь у нас есть чистый монорепозиторий, с которым будем работать дальше.

> **Схема текущего состояния:**
> ```
> ai-engineer-course/
> ├── .gitignore
> ├── README.md
> ├── kompas3d-mcp/
> ├── carverall-mcp/
> └── opencode-config/
>     └── package.json
> ```

---

### ❓ Вопросы для самопроверки (Модуль 0.1)

1. Из каких трёх основных частей состоит архитектура нашего проекта? Кратко опиши роль каждой.
2. Почему мы выбрали связку DeepSeek + YandexGPT, а не ChatGPT?
3. Как проверить, что Node.js установлен и его версия не ниже 20?
4. Зачем в нашем монорепозитории три папки, а не один большой сервер?
5. В каких случаях тебе пригодится Docker при разработке MCP-серверов?

### 🛠 Практическое задание (Модуль 0.1)
- Убедись, что Node.js, npm, Git и Docker установлены и выводят корректные версии.
- Создай монорепозиторий `ai-engineer-course` по описанной структуре, проинициализируй Git и добавь `.gitignore`.
- Сделай скриншот дерева папок (команда `tree -L 2` или `ls -R`) и сохрани его.

---

## Модуль 0.2 — Знакомство с OpenCode

### 2.1 Что такое OpenCode и зачем он нам

**OpenCode** — это open-source терминальный AI-ассистент с агентной архитектурой. Он живёт прямо в твоём терминале и умеет:
- Общаться с различными LLM-провайдерами (DeepSeek, YandexGPT, OpenAI и др.).
- Выполнять многошаговые планы: модель говорит «сделай A, потом B», а OpenCode сам вызывает инструменты, анализирует результаты и решает, что делать дальше.
- Подключать любые MCP-серверы через стандартный протокол Model Context Protocol — это как универсальный USB для AI-инструментов.

**Чем он отличается от Aider, Claude Code и прочих?**
- OpenSource и полностью настраиваемый (ты не привязан к одному вендору).
- Нативная поддержка MCP из коробки — не нужно писать плагины.
- Работает в терминале и легко встраивается в CI/CD.

В нашем курсе OpenCode будет «мозгом»: он понимает твои производственные задачи, планирует цепочку «спроектировать → вырезать» и последовательно дёргает наши кастомные MCP-серверы.

### 2.2 Установка и первый запуск

Установим OpenCode глобально через npm:
```bash
npm install -g @opencode-ai/opencode
```
После установки проверь версию:
```bash
opencode --version
# > opencode/1.2.3 linux-x64 node-v20.11.0
```

> **Примечание для Windows:** используй PowerShell от имени администратора, если возникают проблемы с правами. Вместо `opencode` команда может быть `opencode.cmd`.

Теперь попробуем запустить OpenCode без конфигурации:
```bash
opencode
```
Ты увидишь ошибку:
```
Error: Config file not found. Run 'opencode init' to create a default config.
```
Это нормально. OpenCode ищет файл `opencode.json` в текущей директории или в `~/.opencode`. Мы создадим его чуть позже.

Чтобы просто посмотреть доступные команды:
```bash
opencode --help
```
Вывод покажет основные команды и глобальные флаги.

Полезные флаги, которые понадобятся в курсе:
- `--log-level DEBUG` — включает подробное логирование (видно, какие инструменты вызываются, ответы LLM).
- `--print-logs` — выводит логи в stderr (полезно в скриптах).
- `-m, --model <provider/model>` — явно указать модель (например `deepseek/deepseek-chat`).
- `--config <file>` — указать конкретный конфигурационный файл.

> **💡 Plan mode (Tab):** В TUI OpenCode есть режим планирования. Нажми **Tab** — модель только опишет план действий, не внося изменения. Переключись обратно в `Build` (ещё раз Tab) и подтверди выполнение.

### 2.3 Конфигурация OpenCode (`opencode.json`)

OpenCode настраивается через JSON-файл. Вот минимальная структура:

```json
{
  "model": "название_модели",
  "provider": {
    "type": "deepseek | yandexgpt | openai",
    "api_key": "your_api_key_here",
    "base_url": "https://api.deepseek.com/v1"  // опционально
  },
  "system_prompt": "Ты — помощник инженера...",
  "mcp_servers": []
}
```

**Ключевые поля:**
- `model` — идентификатор модели (например, `"deepseek-chat"` или `"yandexgpt/latest"`).
- `provider.type` — тип провайдера. OpenCode использует внутренние адаптеры для каждого.
- `provider.api_key` — API-ключ (можно передавать через переменные окружения `${ENV_VAR}`).
- `provider.base_url` — для self-hosted или кастомных эндпоинтов.
- `system_prompt` — системный промпт, который задаёт роль ассистенту.
- `mcp_servers` — массив конфигураций MCP-серверов (подключим в следующих блоках).

Создадим в папке `opencode-config/` файл `opencode.deepseek.json` с шаблоном:

```json
{
  "model": "deepseek-chat",
  "provider": {
    "type": "deepseek",
    "api_key": "${DEEPSEEK_API_KEY}",
    "base_url": "https://api.deepseek.com/v1"
  },
  "system_prompt": "Ты — инженерный AI-ассистент. Твоя задача — помогать проектировать детали и управлять лазерным гравировщиком.",
  "mcp_servers": []
}
```

Обрати внимание: мы использовали `"${DEEPSEEK_API_KEY}"` — OpenCode поддерживает подстановку переменных окружения. Так ключ не будет светиться в конфиге.

Сейчас запустить с этим конфигом не получится, потому что переменная ещё не задана. Мы сделаем это в Модуле 0.3.

---

### ❓ Вопросы для самопроверки (Модуль 0.2)

1. Какие три ключевые возможности OpenCode делают его идеальным оркестратором для нашего курса?
2. Как работает Plan mode (Tab) в TUI и чем он полезен на этапе разработки?
3. Какое поле конфигурации отвечает за подключение MCP-серверов?
4. Почему в поле `api_key` мы указали `${DEEPSEEK_API_KEY}`, а не сам ключ?
5. Можно ли использовать один файл конфигурации для нескольких LLM-провайдеров? Если нет — как удобно переключаться?

### 🛠 Практическое задание (Модуль 0.2)
- Установи OpenCode глобально, проверь версию (`opencode --version`) и изучи флаги через `--help`.
- Запусти `opencode`, переключись в **Plan mode** (Tab) и посмотри, как меняется интерфейс.
- Создай в папке `opencode-config/` файл `opencode.deepseek.json` по шаблону выше.
- Попробуй выполнить `opencode --config opencode-config/opencode.deepseek.json --help` — ошибок быть не должно, просто выведет справку.
- Сделай скриншот успешного выполнения команды.

---

## Модуль 0.3 — Получение API-ключей и тестовый прогон

### 3.1 Регистрация в DeepSeek и получение API-ключа

DeepSeek предлагает недорогое API, совместимое с форматом OpenAI. Для наших экспериментов хватит пары центов.

1. Перейди на [https://platform.deepseek.com/](https://platform.deepseek.com/) и зарегистрируйся (можно через Google/GitHub).
2. После входа слева выбери раздел **API Keys**.
3. Нажми **Create new key**, скопируй ключ (начинается с `sk-...`).
4. Проверь баланс в разделе **Billing** — пополни при необходимости. Для тестов хватит 1–2 долларов.

> **Типичная проблема:** Если страница не открывается из РФ, используй VPN. API доступно глобально, но веб-интерфейс может быть заблокирован.

Сохрани ключ временно в безопасное место.

### 3.2 Регистрация в Yandex Cloud и получение доступа к YandexGPT

YandexGPT — это российская модель, доступная через Yandex Cloud. Для работы понадобится API-ключ или IAM-токен. Мы будем использовать статический API-ключ для простоты.

1. Зайди в [консоль Yandex Cloud](https://console.cloud.yandex.ru/) и создай новый проект (платёжный аккаунт привязывать не обязательно для стартового гранта, но может потребоваться).
2. Перейди в **IAM** → **Service accounts**, создай сервисный аккаунт с именем `ai-engineer`.
3. Назначь роли: `ai.languageModels.user` (для доступа к YandexGPT) и `editor` (для общего управления).
4. В том же сервисном аккаунте выбери **Create API key**, скопируй значение.
5. Убедись, что сервис **YandexGPT API** активирован: в консоли перейди в "Foundation Models" и нажми "Enable".

Теперь у тебя есть ключ, который выглядит как `AQVN...` или `y0_...`.

### 3.3 Безопасное хранение ключей

**Золотое правило:** никогда не коммитить ключи в Git. Мы будем использовать `.env`-файл.

В корне монорепозитория создай файл `.env`:
```env
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
YANDEX_API_KEY=AQVNxxxxxxxxxxxxxxxx
```

Сразу проверь, что в `.gitignore` есть строка `.env` (мы добавили её ранее).

Также создай образец `.env.example`:
```env
DEEPSEEK_API_KEY=your_deepseek_key_here
YANDEX_API_KEY=your_yandex_key_here
```
Его можно коммитить — коллеги будут знать, какие переменные нужны.

> **Важно для Windows:** если используешь PowerShell, для загрузки переменных из `.env` можно применить утилиту `dotenv-cli` или вручную задать через `$env:DEEPSEEK_API_KEY = "..."`. Мы будем использовать bash-совместимый подход: `export $(cat .env | xargs)` в Linux/macOS. В Windows (Git Bash) работает тот же способ.

Загрузи переменные в текущую сессию:
```bash
cd ai-engineer-course
export $(cat .env | xargs)   # Linux/macOS / Git Bash
# Проверим:
echo $DEEPSEEK_API_KEY
```

### 3.4 Настройка OpenCode для DeepSeek и YandexGPT

У нас уже есть конфиг для DeepSeek. Осталось создать конфиг для YandexGPT. В папке `opencode-config/` создай `opencode.yandex.json`:

```json
{
  "model": "yandexgpt/latest",
  "provider": {
    "type": "yandexgpt",
    "api_key": "${YANDEX_API_KEY}",
    "base_url": "https://llm.api.cloud.yandex.net/foundationModels/v1"
  },
  "system_prompt": "Ты — инженерный AI-ассистент. Твоя задача — помогать проектировать детали и управлять лазерным гравировщиком.",
  "mcp_servers": []
}
```

Обрати внимание: `base_url` для YandexGPT оканчивается на `/v1`, но OpenCode сам добавит нужные пути.

### 3.5 Тестовый прогон с DeepSeek

Убедись, что переменная `DEEPSEEK_API_KEY` загружена, и выполни:

```bash
opencode --config opencode-config/opencode.deepseek.json
```

Откроется интерактивная сессия. Ты увидишь приглашение:

```
🤖 OpenCode (deepseek-chat) > _
```

Введи первый запрос:
```
> Привет! Как дела?
```
Ожидаемый ответ (пример):
```
Привет! Я в порядке, готов помогать с проектированием. Что будем создавать?
```

Теперь проверим базовый Function Calling (хотя инструментов ещё нет, модель может просто ответить):
```
> Сколько будет 2+2?
```
Ответ DeepSeek:
```
2 + 2 = 4.
```
Если модель отвечает «4», значит всё работает. Для выхода напиши `exit` или нажми Ctrl+C.

> **Типичные проблемы:**
> - **Ошибка 401 (Unauthorized):** проверь, что API-ключ правильный и переменная загружена. Используй `echo $DEEPSEEK_API_KEY` для проверки.
> - **Пустой ответ или обрыв связи:** DeepSeek иногда таймаутит при высокой нагрузке. Попробуй ещё раз или проверь сеть/VPN.

### 3.6 Тестовый прогон с YandexGPT

Аналогично загружаем `YANDEX_API_KEY` и запускаем:

```bash
opencode --config opencode-config/opencode.yandex.json
```

Введи:
```
> Привет, представься и скажи, на каком языке ты общаешься.
```
YandexGPT ответит что-то вроде:
```
Я YandexGPT, ваш инженерный ассистент. Общаюсь на русском языке.
```

Проверь вычисление:
```
> Сколько будет 2+2?
```
Модель должна ответить «4». Это подтверждает базовую работу.

### ✅ Чекпоинт: «Готовность к Блоку 1»

Поздравляю! Ты достиг состояния, когда:
- Все инструменты (Node.js, Git, Docker) установлены.
- OpenCode работает с DeepSeek и YandexGPT.
- Ты можешь запустить интерактивный диалог и получить осмысленный ответ от обеих моделей.
- Конфигурационные файлы готовы к добавлению MCP-серверов.

---

### ❓ Вопросы для самопроверки (Модуль 0.3)

1. Где найти раздел API Keys в панели DeepSeek и как пополнить баланс?
2. Какую роль необходимо выдать сервисному аккаунту Yandex Cloud для работы с YandexGPT?
3. Почему переменные окружения для ключей мы прописываем в отдельном `.env` файле, а не в JSON-конфиге?
4. Что означает ошибка `401 Unauthorized` при запуске OpenCode и как её исправить?
5. Можно ли использовать одновременно оба провайдера в одном экземпляре OpenCode? Если нет, как быстро переключаться?

### 🛠 Практическое задание (Модуль 0.3)
- Получи API-ключи DeepSeek и Yandex Cloud.
- Создай `.env` и `.env.example` в корне проекта, загрузи переменные.
- Запусти OpenCode поочерёдно с каждым провайдером, выполни по 3–4 реплики (например: «Как тебя зовут?», «Реши уравнение x^2 = 16», «Сгенерируй случайное число от 1 до 100»).
- Сделай скриншоты обоих диалогов и сохрани их.

---

## 📚 Домашнее задание (Блок 0)

**Цель:** закрепить навыки настройки и общения с моделями через OpenCode.

1. **Настрой оба провайдера** — DeepSeek и YandexGPT — с отдельными конфигурационными файлами и `.env`.
2. **Выполни по 5 осмысленных запросов** к каждому провайдеру. Темы:
   - Инженерные расчёты (например, «Рассчитай площадь круга диаметром 10 см»).
   - Генерация текста (напиши краткую инструкцию по созданию 3D-модели болта).
   - Логические задачи («Если на гравировку одной детали уходит 3 минуты, сколько деталей можно сделать за час?»).
   - Проверка понимания контекста (задай уточняющий вопрос после предыдущего ответа).
   - Творческое задание (придумай сам).
3. **Оформи результат:**
   - Приложи скриншоты диалогов (можно отдельными файлами).
   - Приложи содержимое твоих JSON-конфигов.
   - Всё это упакуй в папку `homework-block0/` внутри репозитория.

**Критерий сдачи:** OpenCode стабильно запускается с обоими провайдерами, все запросы получают адекватные ответы, ключи не закоммичены в репозиторий.

---

### 🚨 Типичные проблемы и их решения (общий раздел)

| Проблема | Вероятная причина | Решение |
|----------|-------------------|---------|
| `opencode: command not found` | Глобальный npm-путь не в PATH | Перезагрузи терминал, либо добавь `$(npm -g bin)` в PATH. В Windows проверь `%APPDATA%\npm`. |
| `Error: Config file not found` при запуске | Нет файла конфига или неверный путь | Убедись, что флаг `--config` указывает на существующий файл, либо создай `opencode.json` в рабочей директории. |
| DeepSeek возвращает пустой ответ | Проблемы с сетью или превышена квота | Проверь VPN, баланс в личном кабинете, попробуй позже. |
| YandexGPT отвечает «Permission denied» | Не включён сервис Foundation Models или нет роли | Активируй сервис в консоли, добавь роль `ai.languageModels.user`. |
| Ошибка `401 Unauthorized` | Неверный или просроченный ключ | Пересоздай ключ в панели, проверь, что переменная окружения точно загружена (`echo $API_KEY`). |
| Переменные из `.env` не видны | Не выполнен `export` | Выполни `export $(cat .env | xargs)` в том же терминале, где запускаешь OpenCode. Для Windows используй `Get-Content .env | ForEach-Object { ... }`. |

---

Теперь ты полностью готов к Блоку 1, где мы оживим первый MCP-сервер и заставим OpenCode дёргать настоящий инструмент. Вперёд!